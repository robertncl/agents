# Sweep scope for dependency-updater and dependabot-fixer.
#
# One repo per line, `owner/repo`. Blank lines and `#` comments ignored.
#
# Membership rule: a repo belongs here only if ALL of these hold.
#   1. Not a fork of another public repo.
#   2. Not archived.
#   3. Write (or better) permission for the token running the sweep.
#   4. A previous sweep actually found dependencies or vulnerabilities to fix
#      -- evidenced by a merged/closed dependency or security PR.
#
# Verified 2026-08-14 against `gh repo view` (isFork/isArchived/viewerPermission)
# and `gh search prs --owner robertncl`. Re-verify before adding a line.
# Six more verified 2026-09-16 the same way, prompted by a `~/code/*` sweep
# request: every local checkout under ~/code was checked against all four
# conditions before any of it was added here. Repos rejected in that pass and
# why: PacktPublishing/LLM-Engineers-Handbook, bananalab/Learn-Infrastructure-
# as-Code-with-Terraform, NVIDIA/OpenShell, ps-copilot-sandbox/copilot-
# intermediate-gallery-repo, microsoft/generative-ai-for-beginners,
# hashicorp-education/learn-hcp-packer-get-started -- not robertncl's repos.
# robertncl/juice-shop -- origin 404s, repo renamed or deleted upstream.
# robertncl/{DLS,Learning-resource,argocd,databricks,gitops-helm,
# gitops-kustomize,learn-markdown,python,pytorch,test-agents-2} -- zero
# dependency manifests in the tree, nothing a sweep could ever find.
# robertncl/learn-crypto -- has a package.json but no prior sweep evidence
# (condition 4); swept once as an explicit one-off instead of added here.
#
# To add a repo: prove all four conditions above, then append it with the
# evidence PR in the trailing comment. Do not add a repo speculatively --
# an unproven target costs a full clone-and-resolve pass for nothing.

robertncl/angular              # PR #33-#37 (5 CVE overrides), #41 (8 deps, cooloff)
robertncl/crypto               # PR #27, #43 (deps + Actions pinning)
robertncl/crypto-bot           # PR #9, #10 (cryptography, aiohttp advisories)
robertncl/expense              # PR #2-#9 (8 transitive CVE overrides)
robertncl/hrms                 # PR #50, #63 (Actions re-pin, @hono/node-server CVE)
robertncl/nextjs               # PR #1 (deps + Actions pinned to SHA)
robertncl/pokedev              # PR #3, #4 (uuid, shell-quote CVEs)
robertncl/vue-demo             # PR #56, #58 (cooloff bumps, undici x5 alerts)
robertncl/Hello-world-action   # PR #4 (docker/setup-buildx-action bump)
robertncl/bun-app              # PR #7 (bun runtime bump)
robertncl/dinoquest             # PR #4, #5 (deps), #1 (Actions SHA bump)
robertncl/golden-image          # PR #3-#6 (Actions + deps cooloff bumps)
robertncl/license-management    # PR #1-#3 (deps + Actions cooloff bumps)
robertncl/gh-action-demo        # PR #1, #3-#5 (deps cooloff bumps)
robertncl/payment               # PR #1, #3 (deps + Actions pinned to SHA)
robertncl/multicloud            # PR #2, #3 (deps cooloff bumps)
robertncl/closeclaw             # PR #3, #5, #7, #8 (npm deps cooloff bumps)
robertncl/doom-clone             # PR #4 (Cargo deps cooloff bump)
robertncl/flappy                 # PR #5-#7 (deps cooloff bumps)
robertncl/sonatype               # PR #4 (helm chart deps cooloff bump)
robertncl/test-agents            # PR #1 (deps cooloff bump)
robertncl/WorldCup                # PR #1 (deps cooloff sweep)
robertncl/learn-actions           # PR #14 (deps cooloff sweep)
